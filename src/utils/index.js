import clip_rgb from "./clip_rgb.js";
import limit from "./limit.js";
import type from "./type.js";
import unpack from "./unpack.js";
import last from "./last.js";

const PI = Math.PI;
const TWOPI = PI * 2;
const PITHIRD =  PI / 3;
const DEG2RAD = PI / 180;
const RAD2DEG = 180 / PI;

export {
	clip_rgb,
	limit,
	type,
	unpack,
	last,
	PI,
	TWOPI,
	PITHIRD,
	DEG2RAD,
	RAD2DEG,
};
